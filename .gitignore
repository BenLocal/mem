target/

# Claude Code session-local state (task scheduling locks etc.)
.claude/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Worktrees
.worktrees/

# Local-only Cargo build tuning (e.g. mold linker). Machine-specific —
# CI runners lack mold/clang, so this must never be committed. See the
# file's own header for rationale.
.cargo/

# Recall bench fixtures (gitignored — real transcripts may be sensitive)
bench-fixtures/
target/bench-out/
.playwright-mcp/
admin-page-*.png

/docs/superpowers